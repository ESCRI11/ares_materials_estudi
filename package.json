{
  "name": "ares-materials-estudi",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && node scripts/make-cheatsheet.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "check": "npm run build && node scripts/check-links.mjs && node scripts/check-interactive.mjs",
    "check:links": "node scripts/check-links.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.5",
    "astro": "^7.0.2",
    "prismjs": "^1.30.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "playwright": "^1.62.0",
    "rehype-mermaid": "^3.0.0"
  }
}
